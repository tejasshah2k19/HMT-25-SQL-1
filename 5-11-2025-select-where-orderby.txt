list out all the tables from database ? 
-> check tabs table 
-> select * from tabs; -> name 



Q: list out all the products which is  Available ?
Q: list out all the products which can be sale below 125000 ? 
Q: list out all the TV from products table ? 


based on condition we want read data --> where clause 


select * from products; 
we will get all the products 


Q: list out all the products which is  Available ?
select * from products where isAvailable = true  

Q: list out all the products which can be sale below 125000 ? 
select * from products where price < 125000
select * from products where price <= 125000

Q: list out all the products which can be sale below 125000 and which is Available ? 
select * from products where price <= 125000 and isAvailable = true 

Q: list out all the products which 
    can be sale below 125000 /
    which is Available with 15+ qty /
    which is not outof stock 

select * from products where price <= 125000 OR qty >= 15  OR isAvailable = true ;


-------------------------------------------------------------------

CREATE TABLE PRODUCTS (
    PRODUCT_ID NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    PRODUCT_NAME VARCHAR2(100) NOT NULL,
    QTY NUMBER(10) DEFAULT 0 CHECK (QTY >= 0),
    PRICE NUMBER(10,2) NOT NULL CHECK (PRICE >= 0),
    CATEGORY VARCHAR2(50),
    IS_AVAILABLE CHAR(1) DEFAULT 'Y' CHECK (IS_AVAILABLE IN ('Y', 'N')),
    DISCOUNT NUMBER(5,2) DEFAULT 0 CHECK (DISCOUNT >= 0 AND DISCOUNT <= 100),
    CREATED_AT TIMESTAMP DEFAULT SYSTIMESTAMP
);


INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Laptop', 15, 75000, 'Electronics', 'Y', 10);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Smartphone', 30, 45000, 'Electronics', 'Y', 5);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Washing Machine', 8, 28000, 'Home Appliances', 'Y', 8);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Refrigerator', 5, 35000, 'Home Appliances', 'Y', 12);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Microwave Oven', 10, 15000, 'Home Appliances', 'Y', 6);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Headphones', 50, 2500, 'Accessories', 'Y', 15);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Smartwatch', 25, 12000, 'Wearables', 'Y', 10);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Gaming Console', 12, 60000, 'Electronics', 'N', 20);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Air Conditioner', 6, 42000, 'Home Appliances', 'Y', 7);

INSERT INTO PRODUCTS (PRODUCT_NAME, QTY, PRICE, CATEGORY, IS_AVAILABLE, DISCOUNT)
VALUES ('Bluetooth Speaker', 40, 3000, 'Accessories', 'Y', 18);

----------------------------------------------------------------------------

q1: List all the products 

q2: list all the products which can be buy with budget of 1500 to 15000 

q3: list all the products which belongs to 'Home Appliances' 

q4: list all the products which qty is greater than 20 

q5: list all the products which need more than 40000 RS to buy  

q6: list all the products which are expensive 40000+ and discount is 10+ 





